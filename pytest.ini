# Pytest configuration for deepfake-ViT.
# Run from the repo root:  python -m pytest
# Requires the project environment (torch, timm, etc.) — see requirements.txt.

[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
