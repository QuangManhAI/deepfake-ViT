# ============================================================
# Anti Face Deepfake — dependencies
#
# LƯU Ý QUAN TRỌNG cho RunPod Ubuntu + GPU:
#   torch + torchvision KHÔNG nằm trong file này.
#   Trên Linux chúng được cài RIÊNG với index CUDA (xem scripts/setup_ubuntu.sh):
#       pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   (Trên Mac:  pip install torch torchvision  — bình thường)
#
# Đây là các thư viện thuần Python còn lại.
# ============================================================

numpy>=1.26
pillow>=10.0
tqdm>=4.66
scikit-learn>=1.4
safetensors>=0.4
timm>=1.0.20
matplotlib>=3.8
huggingface_hub>=0.25
