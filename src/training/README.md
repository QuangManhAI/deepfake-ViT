# src/training

Training-loop script entry points (`train.py`, `finetune_lora.py`,
`finetune_compare.py`). Training runs only from scripts — never from
notebooks. See
[agents/rules/LOGGING_CHECKPOINT_RULES.md](../../agents/rules/LOGGING_CHECKPOINT_RULES.md).
