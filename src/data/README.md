# src/data

Dataset loading, transforms, splitting, and dataset build/download/extract
scripts.

- `dinov3_vit.py`-consuming datasets live under `src/` packages; this folder
  holds scripts that build, split, download, and extract the DF40 dataset.
- See [agents/rules/FOLDER_STRUCTURE.md](../../agents/rules/FOLDER_STRUCTURE.md).
