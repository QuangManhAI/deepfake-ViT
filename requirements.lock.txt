# requirements.lock.txt — Pinned (reproducible) versions of requirements.txt.
#
# Pinned at the minimum floors declared in requirements.txt. For a fully
# accurate lock of the real environment, regenerate with:
#   pip freeze > requirements.lock.txt     (inside the GPU .venv)
#
# torch + torchvision are installed SEPARATELY with the CUDA index (NOT here):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   (PyTorch >= 2.7.1 is required for DINOv3; on Mac: pip install torch torchvision)

numpy==1.26.4
pillow==10.4.0
tqdm==4.66.4
scikit-learn==1.4.2
safetensors==0.4.3
timm==1.0.20
matplotlib==3.8.4
huggingface_hub==0.25.2
