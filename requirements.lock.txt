# requirements.lock.txt — Pinned (reproducible) versions of requirements.txt.
#
# Regenerate with (excluding torch, which is installed separately with the
# CUDA index):
#   pip freeze | grep -vE '^(torch|torchvision|torchaudio|nvidia-)' > requirements.lock.txt
#     (inside the GPU .venv)
#
# torch + torchvision are installed SEPARATELY with the CUDA index (NOT here):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   (PyTorch >= 2.7.1 is required for DINOv3; on Mac: pip install torch torchvision)

numpy==2.2.6
pillow==12.3.0
tqdm==4.70.0
scikit-learn==1.7.2
safetensors==0.8.0
timm==1.0.28
matplotlib==3.10.9
huggingface_hub==1.28.0
pandas==2.3.3
pytest==9.1.1
